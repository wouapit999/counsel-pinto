# Copy to .env.local and fill in.
# Free key (no card required): https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Optional. Defaults to gemini-flash-latest, which the free tier covers.
# Use gemini-pro-latest only if your account has quota for it.
# GEMINI_MODEL=gemini-flash-latest
